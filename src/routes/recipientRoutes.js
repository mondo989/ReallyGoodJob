const express = require('express');
const router = express.Router();

// Placeholder routes - to be implemented
router.post('/recipients', (req, res) => {
  res.json({ message: 'Upload recipients CSV - to be implemented' });
});

module.exports = router; 